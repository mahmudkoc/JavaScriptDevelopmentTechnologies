var jqu = require('jquery');
var indexUtils = require('./main');
var authUtils = require('./auth');

$(document).ready(function () {
    indexUtils.showPersons();
    // document.auth = authUtils;
});